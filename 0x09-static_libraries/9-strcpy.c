#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 **_strcpy - prints elements in an array
 *@src: value of source
 *@dest: value of dest
 *Return: (integer)
 */
char *_strcpy(char *dest, char *src)
{
return (strcpy(dest, src));
}

