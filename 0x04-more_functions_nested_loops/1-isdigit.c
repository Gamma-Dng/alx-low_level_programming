#include "main.h"
/**
 *_isdigit - Checks for a digit
 *@c: Character computed for check
 *Return: (1) for digit and (0) for any other
 */

int _isdigit(int c)
{
while (c >= 48 && c <= 57)
{
return (1);
}
return (0);
}







